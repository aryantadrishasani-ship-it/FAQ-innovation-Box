```markdown
# Naive RAG FAQ Bot برای Google Colab  
**یک سیستم پرسش و پاسخ ساده و بهینه بر پایه Retrieval-Augmented Generation (RAG)**

این پروژه یک چت‌بات ساده FAQ فارسی است که با کمترین حجم دانلود و مصرف منابع، در Google Colab اجرا می‌شود. مناسب برای پروژه‌های دانشجویی، دموهای سریع یا ساخت چت‌بات داخلی بر اساس فایل Excel/CSV سوالات متداول.

## ویژگی‌های اصلی
- پشتیبانی عالی از زبان فارسی
- دو حالت LLM:
  - **بهترین کیفیت**: استفاده از OpenRouter (مدل Llama 3.1 8B رایگان) → فارسی فوق‌العاده، بدون دانلود مدل
  - **کاملاً محلی و آفلاین**: مدل سبک Qwen2-0.5B (فقط ~1 GB دانلود یک‌بار)
- مدل Embedding کم‌حجم (~90 مگابایت): `paraphrase-multilingual-MiniLM-L12-v2`
- پایگاه داده وکتوری سبک: ChromaDB (کاملاً در حافظه)
- ورودی: فایل Excel یا CSV با ستون‌های `category`, `question`, `answer`
- رابط تعاملی ساده در Colab

## پیش‌نیازها
- Google Colab (رایگان)
- فایل FAQ به فرمت **Excel (.xlsx)** یا **CSV** با حداقل ستون‌های:
  - `question` (سوال)
  - `answer` (پاسخ)
  - `category` (دسته‌بندی - اختیاری)

> نکته: اگر نام ستون‌ها متفاوت است، در کد خط زیر را اصلاح کنید:
> ```python
> df.columns = ['category', 'question', 'answer']
> ```

## راه‌اندازی سریع

1. به Google Colab بروید و یک نوت‌بوک جدید بسازید.
2. کل محتوای فایل `FAQ-innovation-excel file.py` را در یک سل کپی کنید و اجرا کنید.
3. وقتی از شما خواسته شد، فایل FAQ خود را آپلود کنید.
4. سیستم آماده است! سوالات خود را بپرسید.

### استفاده از OpenRouter (توصیه می‌شود - کیفیت بالاتر)
- در openrouter.ai ثبت‌نام کنید و یک API Key رایگان بگیرید.
- در کد خط زیر کلید خود را قرار دهید:
  ```python
  OPENROUTER_API_KEY = "sk-or-v1-..."  # ← کلید خود را اینجا بگذارید
  ```
- `USE_OPENROUTER = True` را نگه دارید.

### استفاده از مدل محلی (آفلاین)
اگر نمی‌خواهید از API خارجی استفاده کنید:
```python
USE_OPENROUTER = False
```
در اولین اجرا مدل Qwen2-0.5B دانلود می‌شود (~1 GB) و سپس کاملاً آفلاین کار می‌کند.

## ساختار فایل FAQ (نمونه Excel/CSV)

| category       | question                          | answer                                      |
|----------------|-----------------------------------|---------------------------------------------|
| ثبت‌نام        | چگونه ثبت‌نام کنم؟               | برای ثبت‌نام به سایت ... مراجعه کنید...    |
| پرداخت         | روش‌های پرداخت چیست؟            | کارت بانکی، درگاه آنلاین و ...             |
| پشتیبانی       | ساعت کاری پشتیبانی چند است؟     | از ۹ صبح تا ۵ عصر، شنبه تا چهارشنبه       |

## شخصی‌سازی
- تغییر تعداد نتایج جستجو: `TOP_K = 5`
- تغییر مدل OpenRouter: مقدار `OPENROUTER_MODEL` را عوض کنید (لیست مدل‌های رایگان در openrouter.ai)
- تغییر مدل Embedding در صورت نیاز

## محدودیت‌ها
- فقط برای استفاده در Google Colab طراحی شده است (به دلیل `files.upload()`)
- مدل محلی (Qwen2-0.5B) نسبت به Llama 3.1 کیفیت پایین‌تری در فارسی دارد اما کاملاً آفلاین است.

## لایسنس
MIT License - می‌توانید آزادانه استفاده، تغییر و توزیع کنید.

---

**ساخته شده برای ساده‌ترین و سریع‌ترین راه‌اندازی یک چت‌بات FAQ فارسی با RAG**

اگر سوالی داشتید، در Issues مطرح کنید! 🚀
```
